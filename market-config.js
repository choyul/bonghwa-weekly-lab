/* ═══════════════════════════════════════════════════════════════
   market-config.js — 장터 설정. 바뀌는 값은 전부 여기 한 곳에만 둔다.

   장터 자료는 이제 구글 시트가 아니라 서버(bonghwa-lab, D1)에 있다.
   주민이 직접 올리고 고쳐야 하는데 시트는 주민이 쓸 수가 없어서다.
   운영 방법은 docs/market-operation.md 참고.
   ═══════════════════════════════════════════════════════════════ */
window.MARKET_CONFIG = {
  /* 서버 주소. 비워 두면 lab-api.js 가 쓰는 곳과 같은 데를 본다. */
  api: '',

  cacheMinutes: 5,          /* 서버 자료를 다시 받기까지 (§6) */
  soonHours: 6,             /* '마감 임박' 배지 기준 (§4-7) */
};
